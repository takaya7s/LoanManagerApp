# 実装メモ

## 画面共通仕様

- 全画面の基準フォントは Yu Gothic UI 12pt
- ローン登録・編集画面の入力コントロールは Yu Gothic UI 14pt
- DataGridViewの行高は34px、ヘッダー高は40px

## 主なクラス

- `Program.cs`
  - exe同階層のConfig / Data / Logs初期化
  - 起動日時ログ初期化（exeと同じ階層のLogsフォルダ）
  - JSON設定読み込み
  - SQLite初期化
  - 未処理例外ログ

- `Infrastructure/LoanRepository.cs`
  - SQLiteテーブル作成
  - ローンと返済スケジュールの保存
  - 入金失敗情報管理

- `Services/RepaymentCalculator.cs`
  - 3種類の返済方式（元利均等・元金均等・一括）
  - 元利均等・元金均等に追加できるボーナス払い
  - 月割り / 日割り
  - 1円未満の四捨五入（固定）
  - 月末・土日調整

- `Forms/MainForm.cs`
  - ローン一覧
  - 返済スケジュール
  - 入金失敗登録
  - 表示後の実サイズに基づくSplitContainer初期化

- `Forms/LoanEditForm.cs`
  - ローン登録・編集
  - 返済期間指定 / 毎月の金額指定
  - ボーナス払いチェックボックスと設定欄
  - 返済額（元金） / 利息 / お支払い額を分けた複数行の概算表示

## 配布確認

配布時は `bin\Release\net48` またはVisual Studioの出力フォルダを基準に、次を確認してください。

- `LoanManagerApp.exe`
- `LoanManagerApp.exe.config`
- `System.Data.SQLite.dll`
- SQLiteのネイティブDLL
- その他、出力された依存DLL

開発PC上のプロジェクトフォルダからDLLを手作業で選別せず、Release出力フォルダ一式を別フォルダへコピーして起動試験してください。

## 端数処理

- 端数処理方法の選択機能はありません。
- `RoundingHelper.RoundToYen` で1円未満を四捨五入します。
- 50銭未満は切り捨て、50銭以上は1円へ切り上げます。
- JSON設定およびSQLiteのローンテーブルには端数処理項目を保存しません。


## 1.0.5 変更内容

- 初期値: 名称「無題」、借入額1,000,000円、年利15%
- 年利入力は最大2桁まで許可し、整数値は小数点なしで表示
- 返済期間指定と毎月返済額指定を選択可能
- 端数処理の画面表示を削除
- Config / Data / Logsをexeと同じ階層に保存
- 旧DB互換処理なし


## 1.0.6 変更内容

- ボーナス払いを返済方式からチェックボックスへ分離
- 元利均等返済と元金均等返済の両方でボーナス払いを利用可能
- 返済方式に括弧付き説明を追加
- `RepaymentSchedule.RepaymentAmount` を追加
- 返済額は通常返済分、お支払い額はボーナス払いを含む実支払合計として表示
- 旧DB互換処理なし。新しいDBスキーマのみを使用


## 1.0.7 変更内容

- 返済額を元金返済分、お支払い額を元金と利息の合計へ再定義
- 元利均等の金額指定は毎月のお支払い額として計算
- 元金均等の金額指定は毎月の元金返済額として計算
- 返済スケジュールの重複していた元金列を画面から削除
- 概算欄を4行化
- 入力欄を14ptへ拡大し、フォーム・入力コントロールを調整


## 1.0.8 変更内容

- `LoanRepository.ReadLoan` で `PrincipalAmount` を読み込んでいなかった不具合を修正
- ローン一覧では返済方式を短い名称で表示
- 登録・編集画面とローン詳細では従来どおり説明付き表示
- 既存DBの再作成は不要（DBには借入額が正しく保存されており、読み込み処理のみの不具合）

## 1.0.9 変更内容

- ローン一覧は年利以降を「次回返済日、お支払い額、推定残高、残回数、総お支払い額」の順に表示
- 初回返済額・初回お支払い額の一覧用モデル項目を削除し、次回お支払い額を追加
- メニューバーの設定ファイル項目を削除
- 借入額を NumericUpDown から右寄せ TextBox に変更
- 借入額はフォーカス取得時にカンマを除去し、フォーカス喪失時にカンマを付加
- 画面背景クリック時にも借入額欄からフォーカスを外して整形
- settings.example.json を削除
## 1.0.10 変更内容

- 返済スケジュール右上に基準日用の `DateTimePicker` と「本日に戻す」ボタンを追加
- ローン一覧の次回返済日・次回お支払い額・推定残高・残回数をシミュレーション日基準で再取得
- 返済スケジュールの「予定・返済日・経過済み」と「残りの返済のみ表示」をシミュレーション日基準で判定
- シミュレーション時は日付欄を黄色、状態文字を濃赤・太字にし、返済スケジュールタブにも状態を表示
- シミュレーション日はメモリ上だけで保持し、DB・JSONには保存しない


## 1.0.11 変更内容

- MainForm / LoanEditForm / PaymentFailureFormをWinFormsデザイナー形式へ変換
- 各フォームにDesigner.csとresxを追加
- デザイナー読み込み用の引数なしコンストラクターを追加
- MainFormのDataGridView列定義をDesigner.csへ移動
- scheduleHeaderを左列AutoSize・右列Percent 100%へ変更
- 日付シミュレーションUIを右端に固定し、左側操作欄に押し出されないように修正
- デザイン時にリポジトリが未設定でもイベント処理が実行されないようガードを追加


## 1.0.11.2 変更内容

- LoanEditFormの右側説明列を拡張し、毎月のお支払い額の説明ラベルを1行固定表示へ変更
- ボーナス払い有効時の説明文を短縮し、縦スクロールバー表示時にも行高が変化しないよう修正
- ボーナス払い設定GroupBox/TableLayoutPanelへGrowAndShrinkと最小高さを設定
- ボーナス払い設定表示時、画面の作業領域内でフォームを自動的に縦方向へ拡張
- スクロール領域の最小高さをフォーム内容のPreferredSizeに合わせて更新

## 1.0.12.0 変更内容

- ローン一覧を `BorrowDate ASC, PrincipalAmount DESC, Id ASC` で取得
- ボーナス払い設定をチェックボックスから「なし／年1回／年2回」のラジオボタンへ変更
- `BonusPaymentFrequency` enumを追加し、SQLiteの保存列を `BonusPaymentFrequency` へ変更
- `BonusPrincipalAmount` を指定月1回ごとの追加元金として計算
- 旧方式のボーナス対象元金総額の均等分配と、ボーナス利息の別枠累積を削除
- 年1回では2つ目の月選択UIを非表示、年2回では異なる月を必須化
- 旧DB互換性なし


## 1.0.13.0 変更内容

- ローン一覧を `IsCompleted ASC, BorrowDate ASC, PrincipalAmount DESC, RemainingPaymentCount DESC, Id ASC` で並べ替え
- 完済判定は、基準日時点で残回数0かつ推定残高0
- 完済行へ薄灰色の背景と灰色の文字色を設定
- 返済スケジュールの「調整前」列と表示用プロパティを削除
- 一覧再読込中の `SelectionChanged` を抑止し、選択中LoanIdの復元後に返済スケジュールと詳細を明示的に再読込

## 1.0.14.0 変更内容

- ローン一覧の名称列を260px、返済方式125px、ボーナス払い115px、残回数80pxへ調整
- ローン一覧・返済スケジュールの全金額列を135pxへ統一
- 返済スケジュールの返済対象月列を削除
- 毎月のお支払い額／元金返済額をNumericUpDownから右寄せTextBoxへ変更し、フォーカス時のカンマ除去・フォーカスアウト時の桁区切りを実装
- 月入力の上限に繰り上げ用の12を許可し、11か月の次で年+1・月0へ正規化

